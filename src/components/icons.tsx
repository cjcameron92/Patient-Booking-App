import {
  ArrowRight,
  LayoutDashboard,
  Blocks,
  BookMarked,
  Settings,
  MessagesSquare,
  CreditCard,
  CalendarDays,
  LucideIcon,
} from "lucide-react";

export type Icon = LucideIcon;

export const Icons = {
  dashboard: LayoutDashboard,
  plugins: Blocks,
  referrals: BookMarked,
  settings: Settings,
  billing: CreditCard,
  messages: MessagesSquare,
  schedule: CalendarDays,
  arrowRight: ArrowRight,
};
